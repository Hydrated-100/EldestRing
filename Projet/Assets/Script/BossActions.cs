using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossActions : Entity
{
    // Start is called before the first frame update
    void Start()
    {
        SetHealth(50);
        SetDamages(10);
    }

    private void Update()
    {
        
    }
}
